from setuptools import setup, find_packages
setup(name="chatbridge", version="0.1.0", description="IM Chatbot Infrastructure connecting any LLM to any platform",
      packages=find_packages(), python_requires=">=3.9")
